<?php
// debug enable/disable
$enable_dbg = true;

// PHP session
$es_session = 'etunnel'; // session name
$sessiondir = '/tmp'; // if not defined will be used "data/sessions" as session dir
$session_lifetime = 1800; //in secconds

// default controller
$default_controller = 'main';


$db_dir = '/tmp/';
